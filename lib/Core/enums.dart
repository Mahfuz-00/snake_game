enum Direction { up, down, left, right }
enum GameMode { classic, rapid }
enum RewardType { extraLife, bonusPoints, speedReduction, bonusSpin, shrinkSize }
enum ControlType { button, touch, touchButton }